# OpenAI Configuration (REQUIRED)
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Database Configuration
USE_SQLITE=true
DATABASE_PATH=./data/call_center.db
DATA_DIR=./data

# Agent Configuration
ENABLE_HANDOFFS=true  # Enable multi-agent collaboration (true/false)

# Agent Display Configuration
# Controls how agent names are shown in responses
AGENT_DISPLAY_MODE=full  # Options:
                         # - full: Shows handoff chain [Router → Assistant]: Response
                         # - simple: Shows final agent [Assistant]: Response  
                         # - none: No agent names shown
                         # - last_only: Shows only the last agent in chain

# Optional: Override all agent names with single brand
# AGENT_NAME_OVERRIDE=Co-Pilot  # All responses show as [Co-Pilot]: ...